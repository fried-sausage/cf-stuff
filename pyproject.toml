[project]
name = "cf-stuff"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[project.scripts]
cf-download = "cf_stuff.cf_download.cli:main"
cf-test = "cf_stuff.cf_test.cli:main"

[build-system]
requires = ["uv_build>=0.9.16,<0.10.0"]
build-backend = "uv_build"
